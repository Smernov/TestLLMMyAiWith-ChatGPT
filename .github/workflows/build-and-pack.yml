name: Build & Package Hub

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: [3.11]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Create venv
        run: python -m venv .venv

      - name: Install deps (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
          .venv/bin/python -m pip install --upgrade pip
          .venv/bin/pip install --prefer-binary rich transformers sentencepiece pyinstaller

      - name: Install deps (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          .venv\Scripts\python -m pip install --upgrade pip
          .venv\Scripts\pip install --prefer-binary rich transformers sentencepiece pyinstaller

      - name: Smoke test (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
          if [ -f hub_finetune.py ]; then .venv/bin/python -m py_compile hub_finetune.py
          elif [ -f hub.py ]; then .venv/bin/python -m py_compile hub.py
          else echo "No entry script found" && exit 1
          fi

      - name: Smoke test (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          if (Test-Path hub_finetune.py) { .venv\Scripts\python -m py_compile hub_finetune.py }
          elseif (Test-Path hub.py) { .venv\Scripts\python -m py_compile hub.py }
          else { Write-Error "No entry script found"; exit 1 }
        shell: powershell

      - name: Build (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
          ENTRY=$( [ -f hub_finetune.py ] && echo hub_finetune.py || echo hub.py )
          rm -rf build dist __pycache__ *.spec || true
          .venv/bin/pyinstaller --noconfirm --clean --onedir "$ENTRY"

      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          if (Test-Path hub_finetune.py) { $ENTRY = 'hub_finetune.py' } else { $ENTRY = 'hub.py' }
          Remove-Item -Recurse -Force build,dist,__pycache__ -ErrorAction SilentlyContinue
          .venv\Scripts\pyinstaller --noconfirm --clean --onedir $ENTRY
        shell: powershell

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.os }}-${{ matrix.python-version }}
          path: dist
